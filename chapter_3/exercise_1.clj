;;;; Use the str, vector, list, hash-map, and hash-set functions

(println (str "hello" " " "world"))

(println (vector "hello" "world"))

(println (list "hello" "world"))

(println (hash-map :a "hello" :b "world"))

(println (hash-set "hello" "hello" "world"))
